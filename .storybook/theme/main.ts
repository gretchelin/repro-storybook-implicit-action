import { create } from 'storybook/theming';

export default create({
  base: 'dark',

  brandTitle: 'myDigiLearn Storybook',
  brandUrl: '/',
  brandImage: '/brand/logo.svg',
  brandTarget: '_self',
});
